spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres-dev}:5432/${DB_NAME:acciobuild_ai_dev}
    username: ${DB_USER:dev_user}
    password: ${DB_PASSWORD:dev_password_123}
  redis:
    host: ${REDIS_HOST:redis-dev}
    port: ${REDIS_PORT:6379}

server:
  port: 8085

logging:
  level:
    com.acciobuild.ai: DEBUG
