package com.acciobuild.ai.enums;

/**
 * Classifications of context sources ingested to formulate LLM prompt contexts.
 */
public enum ContextSourceType {
    KNOWLEDGE_CHUNK,
    METADATA_FIELD,
    USER_METADATA
}
