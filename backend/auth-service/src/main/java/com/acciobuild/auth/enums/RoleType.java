package com.acciobuild.auth.enums;

/**
 * Standard user role classifications for platform access management.
 */
public enum RoleType {
    SUPER_ADMIN,
    ORG_ADMIN,
    PROJECT_ADMIN,
    DEVELOPER,
    REVIEWER,
    VIEWER
}
