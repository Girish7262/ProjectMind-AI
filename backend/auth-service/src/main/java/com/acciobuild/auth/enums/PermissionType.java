package com.acciobuild.auth.enums;

/**
 * Standard fine-grained permissions for security mapping.
 */
public enum PermissionType {
    READ,
    WRITE,
    UPDATE,
    DELETE,
    MANAGE_USERS,
    MANAGE_PROJECTS,
    MANAGE_ORGANIZATIONS,
    ADMIN
}
