package com.acciobuild.knowledge.enums;

/**
 * Enum listing types of semantic relations between documents.
 */
public enum RelationshipType {
    REFERENCES,
    DUPLICATES,
    DEPENDS_ON,
    EXTENDS
}
