package com.acciobuild.project.enums;

/**
 * Enum listing supported Git repository hosting providers.
 */
public enum GitProvider {
    GITHUB,
    GITLAB,
    BITBUCKET,
    AZURE_DEVOPS,
    GENERIC_GIT
}
